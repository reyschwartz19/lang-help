'use client'
import Link from 'next/link'
import { ArrowRight, Check, Play, BookOpen } from 'lucide-react'
import { AppShell, ScreenCard, ScreenHeading } from '@/components/app-shell'
const lessons = [{ title: 'Introductions', detail: 'Say hello and introduce yourself', done: true }, { title: 'Daily routines', detail: 'Talk about your morning', done: false }, { title: 'Around town', detail: 'Find your way around Lyon', done: false }, { title: 'Weekend plans', detail: 'Make plans with a friend', done: false }]
export default function LearnPage() { return <AppShell title="Your learning path"><div className="screen-stack"><ScreenCard><ScreenHeading eyebrow="CHAPTER 3 · DAILY LIFE" title="Morning routines" /><p className="screen-copy">Build the vocabulary you need to talk about your day, one practical lesson at a time.</p><div className="progress-track large"><span style={{width:'65%'}} /></div><div className="xp-row"><span>65% complete</span><span>4 lessons left</span></div></ScreenCard><section className="section-block"><ScreenHeading eyebrow="THE ROADMAP" title="Your next lessons" />{lessons.map((lesson, i) => <div className="lesson-card" key={lesson.title}><div className={`lesson-icon ${lesson.done ? 'complete' : ''}`}>{lesson.done ? <Check size={22} /> : <BookOpen size={22} />}</div><div className="lesson-info"><div className="lesson-title-row"><strong>{i + 1}. {lesson.title}</strong><span>{lesson.done ? 'Done' : i === 1 ? 'Next' : 'Locked'}</span></div><p>{lesson.detail}</p></div>{lesson.done ? <Check className="success-icon" size={19} /> : i === 1 ? <Link href="/reader" className="circle-play" aria-label={`Start ${lesson.title}`}><Play size={16} fill="currentColor" /></Link> : <ArrowRight className="muted-icon" size={18} />}</div>)}</section></div></AppShell> }
